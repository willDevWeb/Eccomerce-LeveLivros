<!DOCTYPE html>
<html lang="pt-br">
    <%- include('partials/head.ejs') %>
<body>
    <%- include('partials/header.ejs') %> 
    <main class="container">
      <div class="bg-light ">
        <div class="text-center">
            <h3 class="p-5">Enviar feedback</h3>
            <% if(locals.created) {%>
                <div class="sucess">
                    <span>id do feedback: ticket_<%= created.id_feedback %> </span>
                </div>
                <p>Cadastrado com sucesso, aguarde e logo verificaremos o problema</p>
            <% } %> 
        </div>
        <style>
            div #feed_row{
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }
        </style>
        <div class="row" id="feed_row">
            <div class="col-8">
                <form action="/ticket/feedback" method="post">
                    <div class="form-group p-4">
                        <label for="assunto" class="pb-2">Assunto: </label>
                        <input type="text" name="subject" class="form-control" id="assunto" placeholder="sobre o que se trata">
                    </div>
                    <div class="form-group p-4">
                        <label for="msg" class="pb-2">Mensagem</label>
                        <textarea name="msg" id="msg" style="width: 100%;" rows="6" class="form-control"></textarea>
                    </div>
                    <% if(locals.error) {%>
                        <div class="danger">
                            <span><%= error.msg %></span>
                        </div>
                    <% } %> 
                    <div class="text-center p-5">
                        <input type="submit" class="btn btn-info" value="Enviar">
                    </div>
                </form>
            </div>
        </div>
      </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>